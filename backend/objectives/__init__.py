"""Learning-objective pipelines: CED import (AP courses) and AI extraction
(everything else, via extract.py). Downstream code reads objectives through
Store.list_unit_objectives() and never cares which pipeline produced them."""